-- lua/plugins/web-tools.lua
return {
  "ray-x/web-tools.nvim",
  config = function()
    -- Configuration for web-tools.nvim goes here
    -- For example:
    -- require('web-tools').setup({
    --   live_server = {
    --     port = 8000,
    --     open_browser = true,
    --   },
    -- })
  end,
}
